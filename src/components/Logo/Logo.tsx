import React from "react";
import "./Logo.scss";

interface Props {}

const Logo = (props: Props) => {
  return (
    <a href="*" className="logo">
      YourService
    </a>
  );
};

export default Logo;
